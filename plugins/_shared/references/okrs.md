# OKRs

Shared across skills in this marketplace. Keep this in sync with the live OKR tracking doc
so skills can cross-reference work against it without a separate lookup each time.

## Live execution table

| KR | Status | Owner | Notes |
|---|---|---|---|
| _fill in current-quarter KRs here_ | not started / on track / at risk / blocked / slipped | TBD | |

`ticket-to-design-prompt` and `pilot-launch-plan` both check this table to flag whether a
piece of work maps to a KR, and whether that KR is currently at risk, blocked, or slipped.
If this table is empty, both skills skip the KR cross-check cleanly rather than forcing a
connection.
